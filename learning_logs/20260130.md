# Learning_logs 기준 vs 코드에서 열기
당분간 .md 파일 작성은 vs code에서, 파이썬 코드는 pycharm에서 적을 예정
여기서 쓰는게 파이참에서 반영 되는지 확인

pycharm에서 github 레보지를 올림.
git init
git config user.name
git config user.email

다 하면 깃 허브 들어가서 파이썬 레포지 만들기

# python 수업

## hurdle # 1
```python
def turn_right():
    turn_left()
    turn_left()
    turn_left()
    
def jump:
    for i in range(6):
        move()
        turn_left()
        move()
        turn_right()
        move()
        turn_right()
        move()
        turn_left()
jump()
```

## hurdle 
```python
def turn_right():
    turn_left()
    turn_left()
    turn_left()

while not at_goal():
    if right_is_clear():
       turn_right()
       move()
    elif front_is_clear():
       move()
    
    else:
       turn_left()
```

## right-hand maze solution
```python
while not at_goal():
    if wall_on_right() and wall_in_front():
        turn_left()
    elif wall_on_right() and front_is_clear():
        move()
    elif right_is_clear():
        turn_right()
        move()
```

ch03_indexing -> main